[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "next-unlock-backend"
version = "1.0.0"
description = "Backend logic and VDF parser for Next Unlock Steam Deck Plugin"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
